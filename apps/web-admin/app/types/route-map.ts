export interface RouteMapStation { id: string; stageId: string | null; assetId: string | null; stationNo: number; title: string; longitude: number; latitude: number; triggerRadiusMeters: number | null }
export interface RouteMapSegment { id: string; fromStationId: string; toStationId: string; segmentNo: number; sourceType: number; geometryGeoJson: string | null; distanceMeters: number | null; estimatedMinutes: number | null; status: number; errorMessage: string | null }
export interface RouteMapDetail { routeId: string; museumId: string; title: string; coordinateSystem: number; geometryGeoJson: string | null; geometryStatus: 'ready' | 'station_only' | 'unavailable'; geometryVersion: number; distanceMeters: number | null; estimatedMinutes: number | null; confirmed: boolean; validationMessages: string[]; stations: RouteMapStation[]; segments: RouteMapSegment[] }
